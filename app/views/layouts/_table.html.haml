%table.table.table-hover
  %thead
    %tr
      - header_data.each do |header|
        %th= header
  %tbody
    - data.each do |values|
      %tr
        - values.each do |value|
          %td= value

